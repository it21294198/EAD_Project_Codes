export enum USER_ROLES {
  Admin = "Admin",
  Vendor = "Vendor",
  CSR = "Csr",
  User = "User",
}
export enum WEB_USER_ROLES {
  Admin = "Admin",
  Vendor = "Vendor",
  CSR = "Csr",
}

export enum USER_STATUS {
  ENABLED = 1,
  DISABLED = 0,
}
